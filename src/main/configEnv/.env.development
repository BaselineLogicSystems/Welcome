# .env.development
# (used when you run locally with `npm run dev` or `npm run start`)

NODE_ENV=development
PORT=8018


NOTIFICATION_RCPT=ZacVohs-Consulting@pm.me

# SMTP – replace the placeholders with real values (or keep as fake)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# SSL Port 465
SMTP_USER=user@gmail.com
SMTP_PASS=your-app-password-here

CONVERTKIT_API_KEY=123abc
CONVERTKIT_API_URL=https://api.convertkit.com/v1/subscribers

GITHUB_TOKEN=123abc

GOOGLE_CLIENT_ID=abcd1234
